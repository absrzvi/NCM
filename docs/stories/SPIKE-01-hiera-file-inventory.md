# Spike: [SPIKE-01] hiera_file Plugin Inventory
Status: READY
D-decisions touched: D10 (hiera.yaml parsing)

## Why (from PRD)
FR2 requires the Policy Tree to render hieradata layered per the fleet's `hiera.yaml` file, including keys routed through the `hiera_file` lookup plugin. D10 architecture assumes static reconstruction of these routes. This spike verifies that assumption and produces the inventory file required by D10 implementation.

## Assumptions
- The `nomad_connect` submodule containing the `hiera_file` plugin is accessible and readable.
- The plugin code can be inspected via grep or code review.
- The plugin does NOT contain per-fact conditionals, environment-aware routing, or any dynamic logic that would make static reconstruction diverge from Puppet's runtime resolution.
- If the plugin IS dynamic, this spike will escalate to the architect for ADR-017 rather than proceeding to D10 implementation.

## What to Investigate
Grep the `nomad_connect` submodule for the `hiera_file` plugin implementation. Enumerate every key path routed through this plugin in the alpin and dostoneu fleets. Confirm the plugin contains no conditional logic, no per-fact branches, and no environment-aware routing that would differ per certname or Puppet environment.

Produce two YAML files:
- `bff/config/hiera_file_inventory/alpin.yaml`
- `bff/config/hiera_file_inventory/dostoneu.yaml`

Each file structure:
```yaml
# hiera_file routing inventory for <fleet>
# Generated by SPIKE-01 on <date>
# Plugin source: <path or commit SHA>
routed_keys:
  - key_path: "example::routed_param"
    routed_file: "hieradata/files/example/routed_param.txt"
  # ... (one entry per routed key)
plugin_is_static: true  # or false — if false, this spike fails
notes: |
  Any observations about plugin behaviour, edge cases, or phase-2 concerns.
```

## Pass Criteria
- Plugin contains no conditional logic, no per-fact branches, no environment-aware routing.
- `plugin_is_static: true` in both inventory files.
- All routed keys for alpin and dostoneu are enumerated with their target file paths.
- Inventory files committed to `bff/config/hiera_file_inventory/`.

## Fail Criteria
- Plugin contains dynamic logic → set `plugin_is_static: false` and escalate to architect for ADR-017.
- D10 implementation stories remain BLOCKED until ADR-017 is resolved.

## Affected Files
- bff/config/hiera_file_inventory/alpin.yaml → create (new file)
- bff/config/hiera_file_inventory/dostoneu.yaml → create (new file)
- docs/stories/SPIKE-01-hiera-file-inventory.md → this file (deliverable)

## Deliverables
1. `bff/config/hiera_file_inventory/alpin.yaml` committed
2. `bff/config/hiera_file_inventory/dostoneu.yaml` committed
3. Spike report appended to this file under "## Findings" section
4. Pass/fail verdict clearly stated
5. If fail: ADR-017 opened immediately with plugin findings as context

## Findings
[To be filled by the agent executing this spike]

## Verdict
[PASS | FAIL] — [one-sentence summary]

## Blocks
- STORY-13 (GET /api/policies/tree — D10 parsing)
- STORY-31 (PolicyTree frontend — hiera_file routing display)
- STORY-16 (Parameter history — hiera_file note rendering)

## Acceptance Criteria
- [ ] Given the `nomad_connect` submodule is accessible, when the `hiera_file` plugin source is inspected, then all conditional logic branches (if any) are documented
- [ ] Given the plugin source has been reviewed, when `plugin_is_static` is set to `true`, then no per-fact, per-environment, or per-certname routing logic exists
- [ ] Given the alpin fleet hiera.yaml references `hiera_file`, when all routed keys are enumerated, then every key is listed in `alpin.yaml` with its target file path
- [ ] Given the dostoneu fleet hiera.yaml references `hiera_file`, when all routed keys are enumerated, then every key is listed in `dostoneu.yaml` with its target file path
- [ ] Given both inventory files are complete, when they are committed, then the commit message references SPIKE-01 and the pass/fail verdict

## Definition of Done
- [ ] Both inventory YAML files committed to `bff/config/hiera_file_inventory/`
- [ ] Findings section in this file populated with plugin behaviour summary
- [ ] Pass/fail verdict stated
- [ ] If fail: ADR-017 opened and linked in this file
- [ ] If pass: STORY-13, STORY-31, STORY-16 unblocked and marked READY
